<div class="row">
  <div class="col-xs-3">
    <h2>プロフィール</h2>
    <%= render 'users/profile', user: @user %>

  <% unless @user.id == current_user.id %>
    <%= render 'rooms/enter', room: @room, user: @user %>
  <% end %>

    <h2>投稿</h2>
    <%= render 'musics/form' %>
  </div>

  <div class="col-xs-9" id="music_list_user">
    <h2>Music</h2>
      <%= render 'musics/list', musics: @musics %>
  </div>